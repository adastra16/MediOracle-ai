services:
  - type: web
    name: medioracle-ai
    env: node
    plan: free
    buildCommand: cd frontend && npm install --legacy-peer-deps --include=dev && npm run build && cd ../backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000

